import React, { PropTypes } from 'react';

const ColorItem = ({ className }) => {
    return (

    );
};

ColorItem.displayName = 'ColorItem';

ColorItem.propTypes = {
    className: PropTypes.string,
};

export default ColorItem;
